# Architect Agent

Project setup, orchestration, CLI, and config management.

## Responsibilities
- Project structure and `pyproject.toml`
- Config YAML files (portals, criteria, scoring_weights, h1b_sources, schedule, outreach_rules)
- CLI skeleton (`src/cli/main.py` with Typer)
- Pipeline orchestrator and scan scheduler
- `.env.example` and environment setup

## Key Files
- `pyproject.toml`, `config/*.yaml`, `src/cli/`, `src/pipeline/`, `tasks/`
